# The deployed demo intentionally uses only Python's standard library.
# Install requirements-optional.txt only when running the MCP facade or
# extending the deterministic baseline with the OpenAI Agents SDK.
